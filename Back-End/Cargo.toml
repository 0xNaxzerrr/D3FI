[package]
name = "D3FI"
version = "0.1.0"
edition = "2021"

[dependencies]
# Framework web
axum = "0.7"              # Alternative: actix-web = "4"
tokio = { version = "1", features = ["full"] }
tower = "0.4"
tower-http = { version = "0.5", features = ["trace", "cors"] }

# Ethereum interaction
ethers = { version = "2.0", features = ["ws", "rustls"] }

# Database
sqlx = { version = "0.7", features = ["runtime-tokio", "postgres", "macros", "uuid", "chrono", "bigdecimal"] }
redis = { version = "0.24", features = ["tokio-comp", "connection-manager"] }

# WebSockets
tokio-tungstenite = "0.21"
futures = "0.3"

# Serialization
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

# Logging and monitoring
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
prometheus = "0.13"

# Utilities
chrono = { version = "0.4", features = ["serde"] }
uuid = { version = "1", features = ["v4", "fast-rng", "macro-diagnostics", "serde"] }
dotenvy = "0.15"  
config = "0.13"
anyhow = "1.0"        # Error handling
thiserror = "1.0"     # Error definition
bigdecimal = { version = "0.4", features = ["serde"] }   # For precise financial calculations
rust_decimal = { version = "1.34", features = ["serde"] }

[dev-dependencies]
mockall = "0.12"
tokio-test = "0.4"